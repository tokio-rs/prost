[package]
name = "proto"
version = "0.1.0"
authors = ["Dan Burkert <dan@danburkert.com>"]

[workspace]
members = [
  "proto-derive",
  "proto-plugin",
  "proto-conformance",
]

[dependencies]
byteorder = "1"

[dev-dependencies]
quickcheck = "0.4"
proto-derive = { path = "proto-derive" }
